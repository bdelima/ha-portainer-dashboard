{
  "config": {
    "step": {
      "user": {
        "title": "Portainer Maintenance",
        "description": "One-time setup. Enter the URL where the Portainer actions webapp is reachable (e.g. https://actions.o.pumapants.cc), and pick the mobile_app device(s) that should get the \"update performed\" confirmation push. This integration registers it as a sidebar panel, installs its automation blueprint, registers the perform_update/update_done services, computes the notification click-through URL automatically, and exposes the update/trouble/stale tracking sensors.",
        "data": {
          "webapp_url": "Webapp URL",
          "notify_devices": "Notify device(s)"
        }
      },
      "reconfigure": {
        "title": "Portainer Maintenance",
        "description": "Update the webapp URL and/or notify device(s) for your existing setup.",
        "data": {
          "webapp_url": "Webapp URL",
          "notify_devices": "Notify device(s)"
        }
      }
    },
    "abort": {
      "already_configured": "Portainer Maintenance is already set up.",
      "reconfigure_successful": "Portainer Maintenance was reconfigured successfully."
    }
  }
}
